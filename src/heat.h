#ifndef HEAT_H
#define HEAT_H

#include <glm/glm.hpp>
#include <vector>

namespace heat
{
    static float radius;
    const int circle_vertices = 50;
    const float TWO_PI = glm::pi * 2.0f;
    std::vector<glm::vec2> draw_boundary()
    {
    
    }
}
#endif // HEAT_H
